type RecordDetails = {
  image: string;
  title: string;
  details: string;
  architect: string;
};

export default function RecordDetailsModal(props: RecordDetails) {
  return <h1>Details Modal</h1>;
}
